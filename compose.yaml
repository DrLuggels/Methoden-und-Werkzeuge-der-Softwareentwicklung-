services:
  web:
    image: nginx:alpine
    ports:
      - "8090:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
    restart: unless-stopped

  whoami:
    image: traefik/whoami
    ports:
      - "8091:80"
    restart: unless-stopped
